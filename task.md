# Tasks - Mandatory City Field for New Areas

- [ ] Implement `getCitiesList()` with caching and `addNewAreaToDataSheet()` in `Actions.js` (GAS)
- [ ] Add `"getCities"` command type in `doPost.js` (GAS)
- [ ] Deploy updated Google Apps Script backend using `clasp deploy`
- [ ] Implement `fetchCities()` in `app/lib/api.ts`
- [ ] Create GET `/api/cities` in `app/api/cities/route.ts`
- [ ] Add `city: ''` to `EMPTY_FORM` and add validation in `ClientForm.tsx`
- [ ] Implement City SearchableSelect UI rendering block when `isNewArea` is true in `ClientForm.tsx`
- [ ] Verify build compiles correctly using `npm run build`
- [ ] Commit and push git changes to remote repository

[build]
  command = "next build --webpack"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  # Optional: You can remove or keep this; the flag overrides it
  NEXT_PRIVATE_TURBOPACK = "0"